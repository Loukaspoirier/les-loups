services:
  db:
    image: postgres:16
    container_name: postgres_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: les-loups
    volumes:
      - ./wv_schema.sql:/docker-entrypoint-initdb.d/wv_schema.sql

    ports:
      - "5432:5432"
    networks:
      - app-network

  administration:
    build: ./moteur-administration
    depends_on:
      - db
    networks:
      - app-network

  jeu:
    build: ./moteur-jeu
    depends_on:
      - db
    networks:
      - app-network

volumes:
  pgdata:

networks:
  app-network:
    driver: bridge